# ==========================================================
# AFK SHARDS v2.2 - OPTIMIZED EDITION
# ==========================================================

mysql:
  host: "intel.lumine.buzz"
  port: "3306"
  database: "s162_AfkShards"
  user: "u162_X1hHVdBfGs"
  password: "Kl7VLnx@JwkWIsNt^hudxRRY"
  pool-size: 10
  connection-timeout: 30000

afk-system:
  # Chu kỳ tính thưởng (Nên để 60 giây).
  # Hệ thống sẽ kiểm tra mỗi 5s để cộng dồn tiến độ.
  reward-interval: 60

  # CHẾ ĐỘ 1: AFK TRONG VÙNG (Yêu cầu WorldGuard)
  region-mode:
    enabled: true
    regions:
      - "afk_zone"
      - "vip_pool"
    amount: 0.5  # 1 phút nhận 0.5 Shard
    message: "&a+ %amount% Shards &7(AFK Zone)"

  # CHẾ ĐỘ 2: ONLINE THÔNG THƯỜNG (Global)
  global-mode:
    enabled: true
    amount: 0.1 # 1 phút nhận 0.1 Shard
    message: "&e+ %amount% Shards &7(Online)"

  # RANK BOOSTER (Áp dụng cho cả 2 chế độ)
  multipliers:
    vip: 1.5
    mvp: 2.0

kill-reward:
  enabled: true
  prevent-same-ip: true
  cooldown-seconds: 600
  base-amount: 5.0
  armor-bonus:
    NETHERITE: 20.0
    DIAMOND: 10.0

messages:
  prefix: "&8[&6AfkShards&8] "
  balance: "&7Số dư: &e%amount%"
  reload: "&aConfig reloaded!"
  no-permission: "&cKhông có quyền!"